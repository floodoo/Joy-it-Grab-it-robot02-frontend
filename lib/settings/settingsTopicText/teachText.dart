import 'package:flutter/material.dart';

class TeachText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.only(top: 15),
      child: Text(
        "Teaching",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
